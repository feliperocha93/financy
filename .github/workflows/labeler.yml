name: Labeler

on: [pull_request]

jobs:
  labeler:
    runs-on: github-actions-developers-runner

    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/labeler@v4
        with:
          repo-token: ${{ secrets.GH_TOKEN }}
          sync-labels: true
